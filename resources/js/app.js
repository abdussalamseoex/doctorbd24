import './bootstrap';

// Alpine is now handled by Livewire 3 automatically.
// If you need to access it globally, you can still import it but don't call start().
// import Alpine from 'alpinejs';
// window.Alpine = Alpine;
// Alpine.start();
